# M1 测试记录

- 模块：M1 项目骨架与基础配置模块
- 软件版本：V0.1.0
- 测试日期：2026-08-11
- 测试环境：Windows 11 x64、Python 3.13.14、PySide6 6.8.3
- 自动测试命令：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`

## 覆盖范围

1. 首次启动默认配置生成、字段版本和目录创建。
2. 合法配置保存及重启后加载。
3. 非法配置拒绝、损坏配置备份恢复、原子写入失败保护。
4. 服务器在线、HTTP错误、不可达和未配置状态。
5. PySide6 主窗口离屏创建、离线状态展示和正常关闭。
6. UI、Workflow、Devices、Communication、Calculation 的禁止依赖检查。

## 执行结果

- 结果：通过
- 项目负责人验收：通过（2026-08-11）
- 验收调整：系统设置界面已移除“默认温度”和“默认湿度”输入项；底层配置默认值 23/50 保留，不影响现有配置文件兼容性
- 自动测试：20项通过，0项失败，0项错误
- 最终完整执行耗时：1.739秒
- 默认配置生成：通过，路径为 `D:\Auto\data\config\workstation.json`
- 默认运行目录创建：通过，`data\cache`、`data\logs`、`templates` 均已创建
- 客户端启动：通过，服务器未配置时正常启动并显示“离线”
- 主窗口视觉检查：通过，中文字体、字段、按钮和状态均无截断或重叠
- 系统设置窗口视觉检查：通过，默认温度/湿度输入项已移除，其余M1可编辑配置项可见且布局正常
- 正式硬件/正式Linux服务：不属于M1验收范围，本次未执行
