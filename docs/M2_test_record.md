# M2 测试记录

- 模块：M2 日志系统模块
- 软件版本：V0.2.0 候选验收版
- 测试日期：2026-08-12
- 测试环境：Windows 11 x64、Python 3.13.14、PySide6 6.8.3
- 自动测试命令：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`

## 覆盖范围

1. 业务、审计、通信三通道分离与 JSON Lines 结构。
2. 带时区毫秒时间戳、等级过滤、通用上下文和异常堆栈。
3. 密码、令牌、Authorization 和嵌套敏感字段脱敏。
4. 每日分卷、365 天保留、目录动态变更。
5. 并发写入的单行完整性。
6. 磁盘写入故障注入，确认不向业务调用方抛出。
7. 客户端启动/退出、配置成功/失败、服务器健康检查接入。
8. 按日期、通道、等级和关键字查询，损坏行跳过。
9. PySide6 日志查看窗口只读性、筛选行为和 production 环境入口隐藏。
10. M1 配置、服务器健康检查、UI 和分层回归。

## 当前执行结果

- 代码实现：完成
- 自动测试：38 项通过，0 项失败，0 项错误
- 最终完整执行耗时：1.923 秒
- 日志核心故障注入：通过
- 启动、退出、配置损坏恢复审计和健康检查落盘：通过
- 异常钩子恢复：通过，主窗口创建失败时不污染全局 `sys.excepthook`
- 日志查看窗口视觉检查：通过，中文、筛选控件、表格和完整 JSON 详情无截断或重叠
- 正式界面权限决定：已确认，日志查看只对管理员/维护人员开放；M3 完成前 production 环境隐藏入口
- 现有数据选取界面：M2 期间未修改，按已确认边界留待 M7 整合
- API、数据库、日志上传和仪器通信：不属于 M2 验收范围，本次未实施
- 项目负责人验收：通过（2026-08-12）
